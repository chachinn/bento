name: Wire Bento v23
on: push
permissions:
  contents: write
jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tools/wire-v23.py
      - run: |
          git config user.name chachinn
          git config user.email 140357822+chachinn@users.noreply.github.com
          git add index.html data/library_manifest.json
          git commit -m 'Bento v23: wire Chinese library'
          git push
