const fs=require('fs');let s=fs.readFileSync('app.js','utf8');const fail=m=>{throw new Error(m)};
if(!s.includes('sourceServings:wanted')){const re=/  function addRecipeIngredientsToGrocery\(r,missingOnly=false\)\{.*?\n\n  function generateGroceriesFromWeek/s;if(!re.test(s))fail('grocery ingredient block missing');const n=`  function addRecipeIngredientsToGrocery(r,missingOnly=false,targetServings=null){const pantry=state.pantry.map(p=>p.name),baseServings=Math.max(1,Number(r.servings)||1),wanted=Math.max(1,Number(targetServings)||baseServings),factor=wanted/baseServings;let lines=(r.ingredients||[]).map(line=>Math.abs(factor-1)<.001?line:scaleIngredientLine(line,factor));if(missingOnly)lines=lines.filter(i=>!pantry.some(p=>ingredientMatches(i,p)));const addedIds=[];for(const line of lines){const name=ingredientName(line)||line.trim();if(!name)continue;const ex=state.grocery.find(g=>ingredientMatches(g.ingredientName||g.text,name)&&!g.checked);if(!ex){const g={id:uid('g'),text:line.trim(),ingredientName:name,checked:false,category:groceryCategory(line),source:\`recipe:\${r.id}\`,sourceServings:wanted};state.grocery.push(g);addedIds.push(g.id)}}commit();toast(addedIds.length?\`\${addedIds.length} item\${addedIds.length===1?'':'s'} added for \${wanted} serving\${wanted===1?'':'s'}\`:'Everything is already listed',addedIds.length?()=>{state.grocery=state.grocery.filter(g=>!addedIds.includes(g.id))}:null)}\n\n  function generateGroceriesFromWeek`;s=s.replace(re,n)}
let a="const dg=e.target.closest('[data-detail-grocery]');if(dg){const r=getRecipe(dg.dataset.detailGrocery);if(r)addRecipeIngredientsToGrocery(r,false);return}",b="const dg=e.target.closest('[data-detail-grocery]');if(dg){const r=getRecipe(dg.dataset.detailGrocery);if(r)addRecipeIngredientsToGrocery(r,false,recipeDetailServingTarget);return}";if(!s.includes('addRecipeIngredientsToGrocery(r,false,recipeDetailServingTarget)')){if(!s.includes(a))fail('detail grocery handler missing');s=s.replace(a,b)}
a="const dgm=e.target.closest('[data-detail-grocery-missing]');if(dgm){const r=getRecipe(dgm.dataset.detailGroceryMissing);if(r)addRecipeIngredientsToGrocery(r,true);return}";b="const dgm=e.target.closest('[data-detail-grocery-missing]');if(dgm){const r=getRecipe(dgm.dataset.detailGroceryMissing);if(r)addRecipeIngredientsToGrocery(r,true,recipeDetailServingTarget);return}";if(!s.includes('addRecipeIngredientsToGrocery(r,true,recipeDetailServingTarget)')){if(!s.includes(a))fail('missing-only handler missing');s=s.replace(a,b)}
fs.writeFileSync('app.js',s);
